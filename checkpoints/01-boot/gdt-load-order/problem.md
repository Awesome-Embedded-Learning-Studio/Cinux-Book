# 找 bug:进入保护模式的三步曲

下面是从实模式进入保护模式的核心序列,共 12 行。其中**有 1 行**放错了位置(顺序 bug),偶发 `jmp CODE_SEL:enter_pm` 之后三重复位(QEMU 里表现为莫名重启)。

点击你认为有问题的那一行。

**提示**:问题不在指令本身,而在「这条指令必须先于哪条」。想想加载 GDT、改 CR0 这类敏感操作期间,被中断打断会发生什么。
