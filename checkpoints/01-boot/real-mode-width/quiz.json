{
  "title": "实模式的寄存器宽度",
  "type": "choice",
  "question": "实模式下通用寄存器的有效宽度是?",
  "options": [
    "8 位 —— 与 8086 之前的 8008 一致",
    "16 位 —— 段地址 ×16 + 偏移的最大成分",
    "32 位 —— 保护模式寄存器宽度直接可用",
    "64 位 —— 长模式寄存器复位后即可用"
  ],
  "answer": 1,
  "hints": [
    "想想实模式地址总线 20 根、寻址 1MB 的经典事实,偏移分量是多少位?",
    "`mov eax, ...` 这类 32 位指令在实模式下确实能执行,但这是「未文档化行为」层面;题目问的是体系结构意义上的有效宽度约定。"
  ]
}
