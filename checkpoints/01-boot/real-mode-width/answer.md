## 参考答案

**16 位**。

实模式是 8086 的兼容模式:通用寄存器按 16 位(`ax`/`bx`/`cx`/`dx`/`si`/`di`/`bp`/`sp`)使用,形成地址时用「段值 ×16 + 偏移」合成 20 位线性地址,最大寻址 1MB。

顺带的实践含义:引导扇区代码里做「复制自身到更高地址」这类操作时,16 位偏移跨不过 64KB 段边界,需要手动更新段寄存器——这是 01 卷反复踩的经典坑。32/64 位宽度分别要等保护模式、长模式的段描述符与 EFER.LM 就位后才算数。
