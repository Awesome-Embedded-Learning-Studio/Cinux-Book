## 参考答案

- 引导扇区长度:**512 字节**(0x200)。BIOS 以 CHS 方式读「柱面 0、磁头 1、扇区 1」,传统扇区 512 字节,这段约定不变。
- 结尾魔数:**0x55AA**。物理存储是小端:`0x55 0xAA` 对应偏移 510、511(`dw 0xAA55` 的汇编写法在字节序上等价)。

两个数字都由 BIOS 固件在加载后校验:长度天然受扇区约束,魔数不对则判定「没有可引导设备」。
