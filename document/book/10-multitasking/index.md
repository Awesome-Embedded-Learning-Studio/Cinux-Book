---
title: 10 · 多任务
---

# 10 · 多任务

> 把 Unix 进程模型的三大原语——fork、execve、waitpid——接进内核,再把 034 铺好的「骨架」通电(子进程返回 0、CoW 写时复制、syscall 跨切换正常),最后把通电后的 fork/execve 接进 GUI:点终端图标 → 独立 shell。这一卷是 Cinux 从「一个进程跑到底」走到「真正的多进程桌面」的过程。

## 阅读顺序

- [001 · fork / execve / waitpid:让进程能生、能换、能收尸](001/)
- [002 · 通电:让 fork/exec 真的能跑](002/)
- [003 · 多终端:每个终端一个独立的 shell](003/)
